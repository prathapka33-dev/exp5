name: Use Secret Example

on: [push]

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Access secret
        run: echo "Using secret..."
        env:
          API_KEY: ${{ secrets.API_KEY }}